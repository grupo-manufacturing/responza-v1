export const APP_SHELL_PADDING_CLASS = 'p-3 sm:p-4'

export const APP_MAIN_OFFSET_CLASS = 'pt-16 lg:pt-0'

/** Inbox-style panels that fill the viewport below the app shell padding. */
export const APP_PANEL_HEIGHT_CLASS = 'h-[calc(100dvh-7rem)] lg:h-[calc(100dvh-2rem)]'

/** Panels with an in-page title row (e.g. Gmail). */
export const APP_PANEL_WITH_HEADER_HEIGHT_CLASS =
  'h-[calc(100dvh-7.5rem)] lg:h-[calc(100dvh-5.5rem)]'
